source "https://rubygems.org"

gem "jekyll", "~> 4.3.0"
gem "jekyll-paginate"
gem "bulma-clean-theme", "~> 1.0"

# Add these gems so Ruby can require them:
gem "csv"
gem "logger" # (not strictly required yet, but recommended for Ruby 3.5+)
